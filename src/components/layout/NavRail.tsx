// NavRail is no longer used — navigation is now in (app)/layout.tsx and the landing page header.
// Kept as empty export for any lingering imports.
export default function NavRail() {
  return null;
}
